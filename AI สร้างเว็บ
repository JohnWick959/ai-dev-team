async function generateLanding(idea){

const res = await client.chat.completions.create({
model:"gpt-4o-mini",
messages:[
{
role:"user",
content:`Create a SaaS landing page for ${idea} using HTML`
}
]
})

return res.choices[0].message.content
}